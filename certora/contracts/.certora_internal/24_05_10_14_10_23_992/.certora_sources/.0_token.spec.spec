// erc20 methods
methods {
  function addBalance() external returns (bool) return DISPATCHER(true);

  function withdraw() external returns (bool)   return DISPATCHER(true);
  

  function withdrawAll() external returns (bool) return DISPATCHER(true);
  
}

