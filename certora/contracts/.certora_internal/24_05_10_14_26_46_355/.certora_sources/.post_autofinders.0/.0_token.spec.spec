// erc20 methods
methods {
    // function _.executeOperation(uint256,uint256,address) external => DISPATCHER(true);
    function _.addBalance() external => DISPATCHER(true);
    function _.withdraw() external => DISPATCHER(true);
    function _.withdrawAll() external => DISPATCHER(true);
  
}

