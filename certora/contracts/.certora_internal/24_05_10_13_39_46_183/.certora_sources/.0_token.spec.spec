// erc20 methods
methods {
    function _.addBalance()  external                             returns (uint256) => DISPATCHER(true);
    function _.withdraw()    external                            returns (uint256) => DISPATCHER(true);
    function _.withdrawAll() external                         returns (uint256) => DISPATCHER(true);


}