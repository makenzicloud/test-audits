// erc20 methods
methods {
    function _.addBalance()  external                             returns () => DISPATCHER(true);
    function _.withdraw()    external                            returns () => DISPATCHER(true);
    function _.withdrawAll() external                         returns () => DISPATCHER(true);


}