// erc20 methods
methods {
    function addBalance()  internal                             returns (uint256) => DISPATCHER(true);
    function withdraw()    internal                            returns (uint256) => DISPATCHER(true);
    function withdrawAll() internal                         returns (uint256) => DISPATCHER(true);


}